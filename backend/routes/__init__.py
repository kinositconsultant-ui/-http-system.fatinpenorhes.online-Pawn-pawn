"""FastAPI router modules — extracted from server.py during Phase 2 refactor.

Each domain lives in its own file with its own local models & helpers where
possible. Cross-domain helpers live in `services.py`.
"""
